name: Trigger Netlify Build
on:
  schedule:
    - cron: '0 */12 * * *'
jobs:
  build:
    name: Deploy Vercel Site
    runs-on: ubuntu-latest
    steps:
      - name: Curl request
        run: curl -X POST -d {} $DEPLOY_HOOK
